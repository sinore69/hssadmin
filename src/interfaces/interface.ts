export interface Schedule {
  day: string;
  time: string;
}

export interface Doctor {
  name: string;
  specialization: string;
  contactNumber: string;
  email: string;
  yoe: string;
  schedule: string;
}
