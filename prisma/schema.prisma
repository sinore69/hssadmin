generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model doctors {
  name           String
  contactNumber  String     @id @unique
  email          String
  specialization String
  yoe            Int
  schedule       String
}